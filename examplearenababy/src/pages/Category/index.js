import React from "react";
import { View, Button } from "react-native";

export default function Category() {
    return (
        <View>
            <Button title='MINHAS CATEGORIAS' />
        </View>
    );
}